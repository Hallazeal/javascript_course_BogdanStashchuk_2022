/** ЗАДАЧА - Объявление и вызов функции
 *
 * 1. Объявите функцию, используя ключевое слово "function"
 *
 * 2. Не указывайте парметры функции
 *
 * 3. Внутри функции выведите в консоль своё имя
 *
 * 4. вызовите функцию
 */

function printMyName() {
  const myName = "Nikita";
  console.log(myName);
}

printMyName();
